// Learn more https://docs.expo.io/guides/customizing-metro
const { getDefaultConfig } = require('@expo/metro-config');

const defaulConfig = getDefaultConfig(__dirname);
defaulConfig.resolver.a

module.exports = config;
